# Jenkins API Tool - 项目结构文档

## 1. 项目目录结构

```
jenkins-api-tool/
│
├── src/                              # 源代码目录
│   ├── index.ts                      # SDK 入口文件 (统一导出)
│   │
│   ├── client/                       # 客户端层
│   │   └── jenkins-client.ts         # JenkinsClient 主客户端类
│   │
│   ├── services/                     # 服务层 (核心业务逻辑)
│   │   ├── http-client.ts            # HTTP 客户端封装
│   │   ├── build-service.ts          # 构建服务 (触发/等待/停止/重试/流式日志)
│   │   ├── status-service.ts         # 状态查询服务 (状态/队列/job 列表)
│   │   ├── download-service.ts       # 下载服务
│   │   ├── bandzip-service.ts        # 压缩/解压服务 (Bandizip)
│   │   └── notify-service.ts         # macOS 系统通知服务
│   │
│   ├── workflow/                     # 工作流公共层 (CLI 工具函数,不入 SDK 导出面)
│   │   ├── client-factory.ts         # createClientFromEnv / precheckAuth
│   │   ├── download.ts               # downloadToFile (进度/重试/大小校验)
│   │   ├── run.ts                    # runWorkflow (统一错误兜底/退出码)
│   │   └── jobs.ts                   # JOBS / FILE_SERVER_BASE / DOWNLOADS_DIR
│   │
│   ├── workflows/                    # 打包工作流实现 (业务编排)
│   │   ├── patch.ts                  # 灵活模块打包
│   │   ├── pcx.ts                    # 固定 pcx 模块打包
│   │   ├── version-merge.ts          # 版本清单合并
│   │   ├── pty-pcx.ts                # pty-pcx 完整打包
│   │   ├── gwwy-local.ts             # gwwy uniapp 本地打包 (Git Bash)
│   │   ├── gwwy-online.ts            # gwwy uniapp 线上打包
│   │   └── interactive.ts            # 交互式文件选择器
│   │
│   ├── cli/                          # 统一 CLI 入口
│   │   └── index.ts                  # 子命令路由 + help
│   │
│   ├── types/                        # 类型定义层
│   │   └── index.ts                  # 所有 TypeScript 类型和接口
│   │
│   ├── errors/                       # 错误处理层
│   │   └── index.ts                  # 自定义错误类
│   │
│   ├── utils/                        # 工具层
│   │   ├── logger.ts                 # 日志工具
│   │   └── helpers.ts                # 辅助函数
│   │
│   └── config/                       # 配置层
│       └── index.ts                  # 环境变量加载
│
├── examples/                         # 工作流薄封装入口 (npm script 调用, 实现在 src/workflows/)
│   ├── 01-build-pty-pcx-full-workflow.ts
│   ├── 02-build-pcx-full-workflow.ts
│   ├── 03-build-gwwy-full-workflow.ts
│   ├── 04-build-gwwy-uniapp-online.ts
│   └── build-patch-workflow.ts
│
├── project/                          # vOrange 版本清单文件 (本地管理,不入 git)
├── downloads/                        # 工作流产物下载目录 (不入 git)
│
├── docs/                             # 项目文档
├── dist/                             # 构建输出目录
│
├── .env.example                      # 环境变量示例
├── .gitignore                        # Git 忽略配置
├── package.json                      # 项目配置和依赖
├── tsconfig.json                     # TypeScript 配置
└── README.md                         # 项目说明文档
```

## 2. 架构分层

```
┌─────────────────────────────────────────────────────────────────┐
│                        使用者 (SDK Consumer)                     │
└────────────────────────────┬────────────────────────────────────┘
                             │ 调用
┌────────────────────────────▼────────────────────────────────────┐
│                     入口层 (index.ts)                            │
│  职责: 统一导出所有公开的类型、类和工具函数                        │
└────────────────────────────┬────────────────────────────────────┘
                             │
┌────────────────────────────▼────────────────────────────────────┐
│                   客户端层 (client/)                             │
│  职责: 对外暴露的主入口类,组合各个服务,提供统一 API                │
└────────────────────────────┬────────────────────────────────────┘
                             │ 委托
┌────────────────────────────▼────────────────────────────────────┐
│                    服务层 (services/)                            │
│  ┌──────────────┐ ┌──────────────┐ ┌──────────────┐             │
│  │ BuildService │ │StatusService │ │DownloadService│            │
│  │ 构建触发/轮询 │ │状态查询/解析  │ │产物下载       │            │
│  └──────┬───────┘ └──────┬───────┘ └──────┬───────┘             │
│         └────────────────┼────────────────┘                      │
│                          │ 使用                                  │
│  ┌───────────────────────▼─────────────────────────────────────┐ │
│  │                      HttpClient                              │ │
│  │            HTTP 请求封装 / 认证 / CSRF / 重试                 │ │
│  └─────────────────────────────────────────────────────────────┘ │
└────────────────────────────┬────────────────────────────────────┘
                             │
┌────────────────────────────▼────────────────────────────────────┐
│                    支撑层 (types/errors/utils/config)            │
│  ┌──────────┐ ┌────────┐ ┌─────────────┐ ┌────────┐            │
│  │  Types   │ │ Errors │ │ Logger/Utils│ │ Config │            │
│  │ 类型定义  │ │ 错误类  │ │ 日志/辅助函数│ │ 配置加载│            │
│  └──────────┘ └────────┘ └─────────────┘ └────────┘            │
└─────────────────────────────────────────────────────────────────┘
                             │
┌────────────────────────────▼────────────────────────────────────┐
│                    Jenkins Server (RESTful API)                  │
└─────────────────────────────────────────────────────────────────┘
```

## 3. 模块详细说明

### 3.1 入口模块 - `src/index.ts`

| 属性 | 说明 |
|------|------|
| 路径 | `src/index.ts` |
| 职责 | SDK 统一出口,导出所有公开 API |
| 依赖 | 所有其他模块 |

**导出内容**:

| 类别 | 名称 | 说明 |
|------|------|------|
| 类 | `JenkinsClient` | 主客户端类 |
| 类型 | `JenkinsClientConfig` | 客户端配置类型 |
| 类型 | `BuildParameters` | 构建参数类型 |
| 类型 | `BuildOptions` | 构建选项类型 |
| 类型 | `BuildStatus` | 构建状态枚举类型 |
| 类型 | `BuildTriggerResult` | 构建触发结果类型 |
| 类型 | `BuildCompleteResult` | 构建完成结果类型 |
| 类型 | `BuildStatusResult` | 构建状态查询结果类型 |
| 类型 | `ArtifactInfo` | 产物信息类型 |
| 类型 | `DownloadResult` | 下载结果类型 |
| 类型 | `DownloadAllResult` | 批量下载结果类型 |
| 错误类 | `JenkinsError` | 基础错误类 |
| 错误类 | `AuthenticationError` | 认证错误类 |
| 错误类 | `JobNotFoundError` | Job 不存在错误类 |
| 错误类 | `BuildFailedError` | 构建失败错误类 |
| 错误类 | `TimeoutError` | 超时错误类 |
| 错误类 | `ArtifactNotFoundError` | 产物不存在错误类 |
| 错误类 | `NetworkError` | 网络错误类 |
| 函数 | `loadConfig` | 从环境变量加载配置 |

---

### 3.2 客户端模块 - `src/client/jenkins-client.ts`

| 属性 | 说明 |
|------|------|
| 路径 | `src/client/jenkins-client.ts` |
| 类名 | `JenkinsClient` |
| 职责 | SDK 主入口类,组合所有服务,对外提供统一 API |
| 依赖 | `HttpClient`, `BuildService`, `StatusService`, `DownloadService` |

**核心方法**:

| 方法 | 参数 | 返回值 | 说明 |
|------|------|--------|------|
| `build(jobName, params?, options?)` | Job名称, 构建参数(可选), 构建选项(可选) | `BuildTriggerResult` \| `BuildCompleteResult` | 触发构建。异步返回队列ID,同步( options.wait=true )返回完整构建结果 |
| `getStatus(jobName, buildNumber)` | Job名称, 构建编号或 'last' | `BuildStatusResult` | 查询指定构建的状态信息 |
| `download(jobName, buildNumber, artifactPath, outputDir)` | Job名称, 构建编号, 产物路径, 输出目录 | `DownloadResult` | 下载单个构建产物 |
| `downloadAll(jobName, buildNumber, outputDir)` | Job名称, 构建编号, 输出目录 | `DownloadAllResult` | 下载该构建的所有产物 |
| `getConsoleText(jobName, buildNumber)` | Job名称, 构建编号 | `string` | 获取构建控制台日志文本 |
| `getProgressiveConsoleText(jobName, buildNumber, start?)` | Job名称, 构建编号, 字节偏移 | `{ text, textSize, moreData }` | 增量获取构建日志 (progressiveText) |
| `listJobs(folder?)` | 文件夹路径(可选, 如 'web') | `JobInfo[]` | 列出 job (根目录或指定 folder, 只列一层) |
| `getQueue()` | 无 | `QueueItemInfo[]` | 查看构建队列 |
| `stopBuild(jobName, buildNumber, options?)` | Job名称, 构建编号, crumb 选项 | `void` | 停止进行中的构建 |
| `retryBuild(jobName, buildNumber, options?)` | Job名称, 构建编号, crumb 选项 | `BuildTriggerResult` | 重试已完成的构建 |
| `verifyAuth()` | 无 | `{ authenticated, user?, version?, url }` | 验证 Jenkins 连接和认证是否有效 |

**内部属性**:

| 属性 | 类型 | 说明 |
|------|------|------|
| `config` | `Required<JenkinsClientConfig>` | 合并默认值后的完整配置 |
| `httpClient` | `HttpClient` | HTTP 客户端实例 |
| `buildService` | `BuildService` | 构建服务实例 |
| `statusService` | `StatusService` | 状态查询服务实例 |
| `downloadService` | `DownloadService` | 下载服务实例 |
| `logger` | `Logger` | 日志记录器 |

---

### 3.3 HTTP 客户端模块 - `src/services/http-client.ts`

| 属性 | 说明 |
|------|------|
| 路径 | `src/services/http-client.ts` |
| 类名 | `HttpClient` |
| 职责 | 封装 axios HTTP 请求,处理认证、CSRF、错误、文件下载 |
| 依赖 | `axios`, `fs`, `path` |

**核心方法**:

| 方法 | 参数 | 返回值 | 说明 |
|------|------|--------|------|
| `getBaseUrl()` | 无 | `string` | 获取 Jenkins 服务器基础 URL |
| `initCrumb()` | 无 | `Promise<void>` | 获取 CSRF crumb token,用于 CSRF 保护 |
| `get<T>(urlPath, params?)` | 请求路径, 查询参数(可选) | `Promise<T>` | 发送 GET 请求 |
| `post<T>(urlPath, data?, headers?)` | 请求路径, 请求体(可选), 请求头(可选) | `Promise<{data, headers}>` | 发送 POST 请求,返回数据和响应头 |
| `download(urlPath, outputPath, onProgress?)` | 请求路径, 输出路径, 进度回调(可选) | `Promise<void>` | 流式下载文件,支持进度回调 |
| `handleAxiosError(error)` | Axios 错误对象 | `never` (抛出异常) | 统一错误处理,转换为自定义错误类型 |

**错误映射**:

| HTTP 状态码 | 抛出的错误类型 |
|-------------|---------------|
| 401 / 403 | `AuthenticationError` |
| 404 | `JobNotFoundError` / `JenkinsError` |
| 超时 | `NetworkError` |
| 其他 | `JenkinsError` |

---

### 3.4 构建服务模块 - `src/services/build-service.ts`

| 属性 | 说明 |
|------|------|
| 路径 | `src/services/build-service.ts` |
| 类名 | `BuildService` |
| 职责 | 处理 Jenkins 构建触发和完成等待逻辑 |
| 依赖 | `HttpClient`, `StatusService`, `form-data`, `fs` |

**核心方法**:

| 方法 | 参数 | 返回值 | 说明 |
|------|------|--------|------|
| `trigger(jobName, params?, options?)` | Job名称, 构建参数(可选), 构建选项(可选) | `Promise<BuildTriggerResult>` | 触发构建,返回队列 ID |
| `waitForCompletion(jobName, queueId, options)` | Job名称, 队列ID, 轮询选项(含 streamLogs) | `Promise<BuildCompleteResult>` | 轮询等待构建完成; streamLogs 开启时按 progressiveText 增量打印日志 |
| `stopBuild(jobName, buildNumber, options?)` | Job名称, 构建编号, crumb 选项 | `Promise<void>` | 停止进行中的构建 (POST /job/{job}/{n}/stop) |
| `retryBuild(jobName, buildNumber, options?)` | Job名称, 构建编号, crumb 选项 | `Promise<BuildTriggerResult>` | 重试已完成的构建 (POST /job/{job}/{n}/retry) |
| `triggerWithoutParameters(jobName)` | Job名称 | `Promise<number>` | 触发无参数构建 (内部方法) |
| `triggerWithParameters(jobName, params)` | Job名称, 构建参数 | `Promise<number>` | 触发带参数构建 (内部方法) |
| `prepareFormData(params)` | 构建参数 | `Promise<FormData>` | 准备 FormData (处理文件上传) |
| `prepareQueryParams(params)` | 构建参数 | `Record<string, string>` | 准备 URL 查询参数 |
| `extractQueueId(response)` | POST 响应 | `number` | 从响应头 Location 中提取队列 ID |
| `handleBuildTriggerError(error, jobName)` | 错误对象, Job名称 | `never` | 处理构建触发错误 |
| `sleep(ms)` | 延迟毫秒数 | `Promise<void>` | 延迟函数 |

**构建触发策略**:

| 场景 | 请求方式 | 端点 |
|------|----------|------|
| 无参数构建 | POST (application/x-www-form-urlencoded) | `/job/{jobName}/build` |
| 简单参数 (String/Boolean/Choice) | POST (query string) | `/job/{jobName}/buildWithParameters?KEY=VALUE` |
| 含文件参数 | POST (multipart/form-data) | `/job/{jobName}/buildWithParameters` |

**轮询等待逻辑**:

```
1. 轮询队列状态 → GET /queue/item/{queueId}/api/json
   └─ 检查 executable 字段,获取构建编号
2. 轮询构建状态 → GET /job/{jobName}/{buildNumber}/api/json
   └─ 检查 building 字段和 result 字段
   └─ building=false 时构建完成
3. 返回 BuildCompleteResult (包含产物列表)
```

---

### 3.5 状态查询服务模块 - `src/services/status-service.ts`

| 属性 | 说明 |
|------|------|
| 路径 | `src/services/status-service.ts` |
| 类名 | `StatusService` |
| 职责 | 查询构建状态,解析 Jenkins API 响应 |
| 依赖 | `HttpClient` |

**核心方法**:

| 方法 | 参数 | 返回值 | 说明 |
|------|------|--------|------|
| `getStatus(jobName, buildNumber)` | Job名称, 构建编号或 'last' | `Promise<BuildStatusResult>` | 查询构建状态,返回完整状态信息 |
| `getBuildNumberFromQueue(queueId)` | 队列 ID | `Promise<{buildNumber, jobName} \| null>` | 通过队列 ID 获取构建编号 |
| `listJobs(folder?)` | 文件夹路径(可选) | `Promise<JobInfo[]>` | 列出 job (GET /api/json?tree=jobs[name,url,color]) |
| `getQueue()` | 无 | `Promise<QueueItemInfo[]>` | 查看构建队列 (GET /queue/api/json) |
| `parseBuildStatus(data, jobName)` | Jenkins API 原始数据, Job名称 | `BuildStatusResult` | 解析 Jenkins 响应为 SDK 类型 (内部方法) |
| `mapBuildStatus(result, building)` | Jenkins 状态值, building 标志 | `BuildStatus` | 映射 Jenkins 状态到 SDK 状态枚举 (内部方法) |

**状态映射**:

| Jenkins result 值 | building | SDK BuildStatus |
|-------------------|----------|-----------------|
| null/undefined | true | `IN_PROGRESS` |
| 'SUCCESS' | false | `SUCCESS` |
| 'FAILURE' | false | `FAILURE` |
| 'ABORTED' | false | `ABORTED` |
| 'UNSTABLE' | false | `UNSTABLE` |
| 'NOT_BUILT' | false | `NOT_BUILT` |
| 其他 | false | `UNKNOWN` |

---

### 3.6 下载服务模块 - `src/services/download-service.ts`

| 属性 | 说明 |
|------|------|
| 路径 | `src/services/download-service.ts` |
| 类名 | `DownloadService` |
| 职责 | 处理构建产物的下载,支持单个和批量下载 |
| 依赖 | `HttpClient`, `StatusService`, `fs`, `path` |

**核心方法**:

| 方法 | 参数 | 返回值 | 说明 |
|------|------|--------|------|
| `downloadArtifact(jobName, buildNumber, artifactPath, outputDir)` | Job名称, 构建编号, 产物路径, 输出目录 | `Promise<DownloadResult>` | 下载单个产物文件 |
| `downloadAllArtifacts(jobName, buildNumber, outputDir)` | Job名称, 构建编号, 输出目录 | `Promise<DownloadAllResult>` | 下载该构建的所有产物 |
| `ensureDir(dirPath)` | 目录路径 | `void` | 确保目录存在,不存在则递归创建 (内部方法) |

**下载流程**:

```
1. 获取构建信息 (包含产物列表)
   └─ GET /job/{jobName}/{buildNumber}/api/json
2. 验证产物是否存在
3. 构建下载 URL
   └─ /job/{jobName}/{buildNumber}/artifact/{relativePath}
4. 流式下载
   └─ 自动创建目录
   └─ 显示进度日志
5. 返回下载结果 (文件名, 本地路径, 大小, 耗时)
```

---

### 3.7 类型定义模块 - `src/types/index.ts`

| 属性 | 说明 |
|------|------|
| 路径 | `src/types/index.ts` |
| 职责 | 定义所有 TypeScript 类型和接口 |
| 依赖 | 无 |

**导出的类型**:

| 类型名 | 类别 | 说明 |
|--------|------|------|
| `LogLevel` | Type | 日志级别: 'debug' \| 'info' \| 'warn' \| 'error' \| 'silent' |
| `JenkinsClientConfig` | Interface | 客户端配置: url, username, apiToken, timeout, retries, retryDelay, logLevel |
| `FileParameter` | Interface | 文件参数: type='file', path |
| `BuildParameters` | Interface | 构建参数: [key: string]: string \| boolean \| FileParameter |
| `BuildOptions` | Interface | 构建选项: wait, pollInterval, maxWaitTime, crumbIssuer, retryOnTimeout, streamLogs |
| `BuildStatus` | Type | 构建状态枚举 |
| `ArtifactInfo` | Interface | 产物信息: fileName, relativePath, size |
| `BuildTriggerResult` | Interface | 构建触发结果: queueId, url, jobName |
| `BuildCompleteResult` | Interface | 构建完成结果: 继承 BuildTriggerResult + buildNumber, status, duration, artifacts |
| `BuildCause` | Interface | 触发原因: shortDescription, userId, userName |
| `Parameter` | Interface | 参数信息: name, value |
| `BuildStatusResult` | Interface | 状态查询结果: jobName, buildNumber, status, timestamp, duration, artifacts, causes, parameters 等 |
| `DownloadResult` | Interface | 下载结果: fileName, localPath, size, duration |
| `DownloadAllResult` | Interface | 批量下载结果: total, success, failed, results |
| `JobInfo` | Interface | job 信息: name, url, color (listJobs 返回) |
| `QueueItemInfo` | Interface | 队列项信息: id, why, taskName, taskUrl, buildNumber, queuedSince |

---

### 3.8 错误处理模块 - `src/errors/index.ts`

| 属性 | 说明 |
|------|------|
| 路径 | `src/errors/index.ts` |
| 职责 | 定义所有自定义错误类 |
| 依赖 | 无 |

**错误类继承关系**:

```
Error
 └─ JenkinsError (基础错误, statusCode)
     ├─ AuthenticationError (认证失败, 401/403)
     ├─ JobNotFoundError (Job 不存在, 404)
     ├─ BuildFailedError (构建失败, buildNumber)
     ├─ TimeoutError (操作超时)
     ├─ ArtifactNotFoundError (产物不存在)
     └─ NetworkError (网络错误)
```

**错误类详情**:

| 错误类 | 说明 | 额外属性 |
|--------|------|----------|
| `JenkinsError` | 基础错误类 | `statusCode?: number` |
| `AuthenticationError` | 认证失败 (用户名或 Token 错误) | 无 |
| `JobNotFoundError` | Job 不存在 | 无 |
| `BuildFailedError` | 构建执行失败 | `buildNumber: number` |
| `TimeoutError` | 操作超时 (等待构建、请求超时) | 无 |
| `ArtifactNotFoundError` | 指定的产物不存在 | 无 |
| `NetworkError` | 网络连接问题 | 无 |

---

### 3.9 日志工具模块 - `src/utils/logger.ts`

| 属性 | 说明 |
|------|------|
| 路径 | `src/utils/logger.ts` |
| 类名 | `Logger` |
| 职责 | 提供分级日志输出 |
| 依赖 | `types/LogLevel` |

**核心方法**:

| 方法 | 说明 |
|------|------|
| `debug(message, ...args)` | 调试日志 |
| `info(message, ...args)` | 信息日志 |
| `warn(message, ...args)` | 警告日志 |
| `error(message, ...args)` | 错误日志 |
| `shouldLog(level)` | 检查日志级别是否启用 (内部方法) |

**日志级别优先级**: `debug(0) < info(1) < warn(2) < error(3) < silent(4)`

**输出格式**: `[LEVEL] YYYY-MM-DD HH:mm:ss - message`

---

### 3.10 辅助函数模块 - `src/utils/helpers.ts`

| 属性 | 说明 |
|------|------|
| 路径 | `src/utils/helpers.ts` |
| 职责 | 提供通用的格式化工具函数 |
| 依赖 | 无 |

**导出的函数**:

| 函数 | 参数 | 返回值 | 说明 |
|------|------|--------|------|
| `formatDuration(ms)` | 毫秒数 | `string` | 格式化持续时间,如 "1h 23m 45s", "5m 30s", "45s" |
| `formatTimestamp(timestamp)` | 时间戳 (ms) | `string` | 格式化时间戳为本地时间字符串 (中文格式) |
| `formatFileSize(bytes)` | 字节数 | `string` | 格式化文件大小,如 "2.50 MB", "100.00 KB" |
| `stripTrailingSlash(url)` | URL 字符串 | `string` | 去除 URL 末尾的斜杠 |
| `escapeRegExp(s)` | 字符串 | `string` | 转义正则特殊字符 (用于把动态字符串拼进正则) |

---

### 3.11 配置加载模块 - `src/config/index.ts`

| 属性 | 说明 |
|------|------|
| 路径 | `src/config/index.ts` |
| 职责 | 从环境变量加载 Jenkins 配置 |
| 依赖 | `dotenv`, `types`, `utils/helpers` |

**导出的函数**:

| 函数 | 参数 | 返回值 | 说明 |
|------|------|--------|------|
| `loadConfig()` | 无 | `Partial<JenkinsClientConfig>` | 从 .env 文件读取环境变量并转换为配置对象 |

**支持的环境变量**:

| 环境变量 | 配置字段 | 类型 | 说明 |
|----------|----------|------|------|
| `JENKINS_URL` | url | string | Jenkins 服务器地址 (自动去除末尾斜杠) |
| `JENKINS_USERNAME` | username | string | 用户名 |
| `JENKINS_API_TOKEN` | apiToken | string | API Token |
| `JENKINS_TIMEOUT` | timeout | number | 请求超时时间(ms) |
| `LOG_LEVEL` | logLevel | LogLevel | 日志级别 |

---

## 4. 工作流层与示例入口说明

工作流的业务实现统一在 `src/workflows/`，`examples/` 下的文件只是薄封装入口（供 npm script 调用），统一 CLI `npm run jenkins -- <子命令>` 也转发到同一实现。

| 实现文件 | 入口 | 说明 |
|----------|------|------|
| `src/workflows/patch.ts` | `npm run patch` / `jenkins patch` | 灵活模块打包：按版本清单全量打包后按 --module 裁剪 |
| `src/workflows/pcx.ts` | `npm run pcx` / `jenkins pcx` | 固定 pcx 模块打包 (patch 的固定参数版) |
| `src/workflows/version-merge.ts` | `npm run merge` / `jenkins merge` | 版本清单合并：缺省读 merge/ 目录两个默认文件 (vOrange + orangePatchVersion.txt)，也可 --vorange/--patch 指定 |
| `src/workflows/pty-pcx.ts` | `npm run pty-pcx` / `jenkins pty-pcx` | pty-pcx 构建 + workspace 下载 + 解压 + 重压缩 |
| `src/workflows/gwwy-local.ts` | `npm run gwwy` / `jenkins gwwy` | 本机 Git Bash 构建 uniapp 并压缩 (支持 --branch) |
| `src/workflows/gwwy-online.ts` | `npm run gwwy-online` / `jenkins gwwy-online` | 触发 Jenkins 线上打包并下载产物 (支持 --branch/--head) |
| `src/workflows/interactive.ts` | — | patch 缺 --project 时的交互式文件选择 |

公共层 `src/workflow/`：`createClientFromEnv()`（环境变量组装客户端）、`precheckAuth()`（认证预检）、`downloadToFile()`（进度/重试/大小校验下载）、`runWorkflow()`（统一错误兜底 + 失败通知 + 退出码 1）、`jobs.ts`（job 名与文件服务器地址集中）。

## 5. 文件依赖关系

```
index.ts
  ├── client/jenkins-client.ts
  │     ├── services/http-client.ts
  │     ├── services/build-service.ts
  │     │     ├── services/http-client.ts
  │     │     └── services/status-service.ts
  │     ├── services/status-service.ts
  │     │     └── services/http-client.ts
  │     └── services/download-service.ts
  │           ├── services/http-client.ts
  │           └── services/status-service.ts
  ├── types/index.ts
  ├── errors/index.ts
  ├── utils/logger.ts
  ├── utils/helpers.ts
  └── config/index.ts
```
